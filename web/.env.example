# Base URL of the backend API. In local dev this is left blank and Vite proxies
# /api to http://localhost:3000 (see vite.config.ts). In production set it to
# your deployed backend, e.g. https://tahmin-ligi-api.onrender.com/api/v1
VITE_API_URL=
